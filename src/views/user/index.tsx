import React from 'react';
import { Button } from 'antd';
// import './styles';

const User = () => {
    return (
        <div className="user">
            <div>我是user组件</div>
            <Button>增加</Button>
        </div>
    );
};
export default User;
